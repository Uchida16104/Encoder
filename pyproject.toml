[project]
name = "encoder"
version = "1.0.0"
description = "Character encoding inspection, preview and conversion tool for CSV, SQL and text files."
requires-python = ">=3.11"
dependencies = [
  "Django>=5.2,<6.1",
  "charset-normalizer>=3.4,<4",
]

[tool.vercel.scripts]
build = "python manage.py collectstatic --noinput"
